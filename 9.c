#include<stdio.h>
int main()
{
    int x;
    char y;
    float a;
    double b;
    printf ("Size of int is %d ",sizeof(x));
    printf ("\nSize of char is %d ",sizeof(y));
    printf("\nSize of float is %d ",sizeof(a));
    printf("\nSize of double is %d ",sizeof(b));
    return 0;
}